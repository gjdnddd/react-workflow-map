# 맥락노트_리액트워크플로우맵

## 결정 사항
- 2026-07-11: 폴더 위치는 사용자가 지정한 `OneDrive/4. AI/claude/REACT MAP/` 그대로 사용 (기본 규칙인 OneDrive/개발/ 하위가 아님)
- 2026-07-11: 최종적으로 HQ라는 통합 대시보드에 탭 하나로 편입될 예정 → 독립 실행도 가능해야 하고, 추후 임베드도 고려해야 함
- 2026-07-11 (구상 인터뷰 결론):
  - 트리 깊이 무제한 (유닛 안에 또 유닛) — 최하위 노드는 상세 패널로 종료
  - 노드 데이터 필드는 자유 형식, 설계 단계에서 확정
  - 연결선은 방향성 + 관계 라벨 있음
  - Gist 저장 + GitHub 토큰 localStorage 방식 (기존 프로젝트들과 동일 패턴)
  - AI 연동은 "앱 내 자동분석"이 아니라 "채팅에서 Claude에게 부탁 → Gist 직접 편집" 방식. 앱 자체엔 AI 기능 없음.
  - 초기 데이터는 빈 상태(HQ만), 브랜치부터 사용자가 직접 채움
  - 최우선 기능은 방사형 탐색 UI, 편집은 2순위
  - 검색/필터, AI 자동 연결 추천, 권한 설정 등은 이번 버전 제외

## 제약사항
- GitHub Pages 정적 배포 → 백엔드 없음, 클라이언트에서 Gist API 직접 호출
- 모바일 터치 인터랙션 필수 (드릴다운, 확대/축소, 뒤로가기)

## 환경 확인 (2단계, 2026-07-11)
- GitHub 저장소: 신규 생성 필요
- 로컬 개발 환경: Node.js v24.14.1 / npm 11.11.0 확인됨 (설치 완료 상태)
- Gist용 GitHub PAT: 기존 토큰 재사용 (SEED/VR대시보드에서 쓰던 것)

## 합의 내용
- 1단계(구상) 완료 (마스터 컨펌 2026-07-11). 2단계(설계) 완료.
- 3단계 구현 2·3덩어리(Gist 연동 + 편집 UI) 완료 (2026-07-11)

## 트러블슈팅 기록 (2026-07-11)
- React Flow(`@xyflow/react`)의 `fitView`와 내장 엣지 렌더링이 이 자동화 브라우저 환경에서 동작하지 않음
  → 원인: ResizeObserver 기반 노드 측정(`node.measured`)이 이 환경에서 발화하지 않아, 뷰포트 맞춤과 엣지 경로 계산이 모두 실패
  → 조치: 뷰포트는 컨테이너 크기+노드 위치로 직접 계산해 `setViewport()` 호출. 엣지는 React Flow 내장 렌더링 대신 직접 계산한 좌표로 SVG 오버레이(`EdgeOverlay`)를 그려 우회
  → 실제 사용자 브라우저에서는 이 이슈가 재현되지 않을 가능성이 높음(자동화 환경 특유의 문제로 추정)이지만, 어차피 더 안정적인 방식이라 그대로 유지
- Gist 쓰기(PATCH)는 GitHub PAT이 필요해 AI가 직접 테스트 불가 (토큰 입력은 보안 규칙상 AI 금지 행동) → 사용자가 앱에서 "🔑 토큰" 버튼으로 직접 입력 후 저장 테스트 필요
